/**
 * Creategame page controller.
 */

goog.provide('avalon.createpage.CreateCtrl');

/**
 * Create page controller.
 *
 * @param {angular.$location} $location The Angular location service.
 * @constructor
 * @ngInject
 */
avalon.createpage.CreateCtrl = function($location) {

  /**
   * @private{angular.$location}
   */
  this.location_ = $location;
};
